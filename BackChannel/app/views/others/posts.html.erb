<p>You and following users have voted:</p>

<% @votes.each do |vote| %>
    <% if vote.user.id != current_user.id %>
        <div style="margin-left: 20px;"><%= vote.user.username %></div>
        <br>
    <% end%>
<% end %>
<br><br><br>

<td><%= link_to 'Back to the post', post_path(@post) %></td>